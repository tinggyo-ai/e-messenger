{
  "name": "e-messenger-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor shell for E-Messenger mobile apps",
  "scripts": {
    "build:web": "npm --prefix ../web run build -- --mode mobile",
    "sync": "npm run build:web && cap sync",
    "add:android": "cap add android",
    "add:ios": "cap add ios",
    "android": "npm run build:web && cap sync android && cap open android",
    "ios": "npm run build:web && cap sync ios && cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/core": "^6.2.1",
    "@capacitor/ios": "^6.2.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.1"
  }
}
